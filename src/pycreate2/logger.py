
import logging
logging.basicConfig(
    format='[%(name)-15s %(asctime)s,%(msecs)03d] %(levelname)s in <%(funcName)s>: %(message)s',
    datefmt="%H:%M:%S",
    level=logging.DEBUG,
    filename="pycreate2.log",
)
